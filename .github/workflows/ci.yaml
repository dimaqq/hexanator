---
name: CI

on:
    push:
    pull_request:

jobs:
    test:
        runs-on: ubuntu-slim

        steps:
            - uses: actions/checkout@v4
            - uses: astral-sh/setup-uv@v5
            - run: uvx --with tox-uv tox
